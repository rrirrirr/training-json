// The key places to update in app-sidebar.tsx:

// Change in the View Mode Toggle section:
<Button
  variant={viewMode === "month" ? "default" : "outline"}
  size={isOpen ? "sm" : "icon"}
  onClick={() => changeViewMode("month")} // Use context action
  className={cn("w-full", isOpen && "justify-start")}
  aria-label="Block View"
>
  <Calendar className={cn("h-4 w-4", isOpen && "mr-2")} /> {isOpen && "Block View"}
</Button>

// Change in the Dynamic Content section:
<h2 className="text-xs uppercase tracking-wider text-muted-foreground font-semibold mb-2">
  Blocks
</h2>
<div className="space-y-1">
  {monthsForSidebar.map((block) => (
    <button
      key={block.id}
      onClick={() => selectMonth(block.id)} // Use context action
      className={cn(
        "w-full p-2 rounded text-left text-sm transition-colors",
        selectedMonth === block.id
          ? "bg-accent text-accent-foreground"
          : "text-foreground hover:bg-accent/50"
      )}
    >
      {block.name}
    </button>
  ))}
</div>
